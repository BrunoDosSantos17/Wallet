version: '3.7'

services:
  postgres:
    image: postgres:15
    ports:
      - '5433:5432'
    environment:
      - POSTGRES_USER=proposaldb
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=wallet
    volumes:
      - proposaldb_pg_data:/var/lib/postgresql/data

volumes:
  proposaldb_pg_data: